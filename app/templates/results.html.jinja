<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
    <script>
        var code = {{ code }};
    </script>
    <form action="{{ url_for('download', code=code) }}" method="post">
    <label for="code">Download the opinions file</label>
    <button type="submit" name="download" value="download">Download</button>
    <div>
        <img src="{{url_for('static', filename='/Image/code_bar.png')}}">
        <img src="{{url_for('static', filename='/Image/code_pie.png')}}">
    </div>
</body>
</html>